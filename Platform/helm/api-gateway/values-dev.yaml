# helm/api-gateway/values-dev.yaml
# Only overrides — everything else comes from values.yaml.
# CI pipeline updates image.tag on every deploy to main.

image:
  repository: REPLACE_WITH_ACCOUNT_ID.dkr.ecr.us-east-1.amazonaws.com/platform/api-gateway
  tag: latest

replicaCount: 1

resources:
  requests:
    cpu:    "100m"
    memory: "128Mi"
  limits:
    cpu:    "500m"
    memory: "256Mi"
