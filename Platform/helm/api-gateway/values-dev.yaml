# helm/api-gateway/values-dev.yaml
# Only overrides — everything else comes from values.yaml.
# CI pipeline updates image.tag on every deploy to main.

image:
  repository: 444062204470.dkr.ecr.us-east-1.amazonaws.com/platform/api-gateway
  tag: test

replicaCount: 1

resources:
  requests:
    cpu:    "100m"
    memory: "128Mi"
  limits:
    cpu:    "500m"
    memory: "256Mi"
