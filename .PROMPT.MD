I'm building a DevSecOps portfolio project called "platform". Here is the complete structure and what each piece does:
Stack: Python FastAPI microservices (api-gateway, user-service, order-service) → Docker Compose locally → EKS on AWS → Helm charts deployed by ArgoCD (GitOps) → Terraform + Terragrunt for multi-env infra (dev/staging/prod) → GitHub Actions CI (lint → Trivy scan → build → push ECR → update git tag) → Kyverno policies (no-root-containers, require-resource-limits, require-trusted-registry) → Istio service mesh (mTLS, circuit breaker, canary deployments via VirtualService weights)
Project structure:
platform/
├── apps/
│   ├── api-gateway/     # FastAPI router, no DB, forwards to other services
│   ├── user-service/    # FastAPI + SQLAlchemy + Postgres (port 8001)
│   └── order-service/   # FastAPI + SQLAlchemy + Postgres (port 8002), calls user-service to validate users
├── helm/
│   ├── api-gateway/     # Chart.yaml, values.yaml, values-dev/staging/prod.yaml, templates/deployment.yaml + service.yaml
│   ├── user-service/    # same structure
│   └── order-service/   # same structure
├── infra/
│   ├── terraform/       # modules/eks + modules/ecr, bootstrap/ for S3+DynamoDB state, versions.tf with S3 backend
│   └── terragrunt/      # root terragrunt.hcl (S3 backend once), dev/ staging/ prod/ each with 15-line override
├── policy/
│   └── kyverno/         # no-root-containers.yaml, require-resource-limits.yaml, require-trusted-registry.yaml
├── gitops/
│   ├── argocd/          # app-of-apps.yaml + per-service Application manifests pointing to helm/ charts
│   └── istio/           # namespace-labels.yaml, peer-authentication.yaml, destination-rules.yaml, virtual-services.yaml, gateway.yaml
├── .github/
│   └── workflows/
│       └── ci.yaml      # 4 jobs: lint → scan (Trivy matrix) → build-push (ECR, SHA tag) → deploy (sed image tag, git commit [skip ci])
├── docker-compose.yaml  # 5 containers: 3 services + 2 postgres DBs, user/order-service not exposed to host
├── SETUP.md             # complete setup guide, every command and placeholder to change
└── CONCEPTS.md          # what every piece does with ASCII diagrams
Key details:

AWS region: us-east-1, single EKS cluster with dev/staging/prod namespaces (not 3 clusters — cost)
Images tagged with git SHA (never latest), CI updates helm/SERVICE/values-dev.yaml image.tag on every merge to main
ArgoCD selfHeal=true (reverts manual cluster changes back to git), App of Apps pattern
Kyverno in Enforce mode (hard block, not audit)
Istio STRICT mTLS across all namespaces, canary via VirtualService weights (stable/canary subsets)
Terragrunt derives environment name from folder name automatically, separate state file per env in same S3 bucket
Placeholders still in files: REPLACE_WITH_ACCOUNT_ID (AWS account ID) and YOUR_USERNAME (GitHub username)

My question is: [YOUR QUESTION HERE]